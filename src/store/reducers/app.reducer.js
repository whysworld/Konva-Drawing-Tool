
const initial = {
};

export default (state = initial, action) => {
    switch (action.type) {
        // case types.RECEIVE_DOW:
        //     return {
        //         ...state,
        //         dow: action.payload
        //     };
   
        default:
            return state
    }
}